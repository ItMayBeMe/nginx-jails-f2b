# Fail2Ban configuration file
#
# Regexp to catch 400 nginx responses


[Definition]

failregex = ^<HOST> -.*?- \[.*\] \"[A-Z]+ .*?HTTP\/.*?\" 400 \d+ \".*?\" \".*?\"$ 
ignoreregex =

# Author: ItMayBeMe
